{
  "name": "analog",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/analog/src",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@analogjs/platform:vite",
      "defaultConfiguration": "production",
      "outputs": ["{workspaceRoot}/dist/apps/analog"],
      "options": {
        "configFile": "apps/analog/vite.config.ts",
        "main": "apps/analog/src/main.ts",
        "outputPath": "dist/analog/client",
        "tsConfig": "apps/analog/tsconfig.app.json"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "sourcemap": false,
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@analogjs/platform:vite-dev-server",
      "continuous": true,
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "analog:build",
        "port": 4200
      },
      "configurations": {
        "development": {
          "buildTarget": "analog:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "analog:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "analog:build",
        "port": 4200,
        "staticFilePath": "dist/analog/browser",
        "spa": true
      }
    },
    "test": {
      "executor": "@analogjs/vitest-angular:test",
      "options": {
        "config": "vite.config.ts"
      }
    }
  }
}
